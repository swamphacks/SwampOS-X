export type Size = {
	w: number;
	h: number;
};

export type Resize = {
	w?: number;
	h?: number;
};

export type Position = {
	x: number;
	y: number;
};

export const GRID: [number, number] = [4, 4];
